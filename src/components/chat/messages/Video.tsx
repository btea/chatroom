import React, { ReactElement } from "react";
import styles from './video.module.less';

export default function Video(props: InfoType.info): ReactElement {
    return (
        <div className={styles['video-msg']}>
            
        </div>
    )
}
